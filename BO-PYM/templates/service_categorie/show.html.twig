{% extends 'base.html.twig' %}

{% block title %}ServiceCategorie{% endblock %}

{% block body %}
    <h1>ServiceCategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service_categorie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ service_categorie.name }}</td>
            </tr>
            <tr>
                <th>Primary_color</th>
                <td>{{ service_categorie.primaryColor }}</td>
            </tr>
            <tr>
                <th>Img_url</th>
                <td>{{ service_categorie.imgUrl }}</td>
            </tr>
            <tr>
                <th>Action</th>
                <td>{{ service_categorie.action }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('service_categorie_index') }}">back to list</a>

    <a href="{{ path('service_categorie_edit', {'id': service_categorie.id}) }}">edit</a>

    {{ include('service_categorie/_delete_form.html.twig') }}
{% endblock %}
